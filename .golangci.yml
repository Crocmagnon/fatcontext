version: "2"
issues:
  fix: true
linters:
  default: all
  disable:
    - depguard
    - exhaustruct
    - wsl
    - noinlineerr
    - gomodguard
  settings:
    cyclop:
      max-complexity: 20
    funlen:
      lines: -1
formatters:
  enable:
    - goimports
    - gofmt
    - gofumpt
    - golines
  settings:
    goimports:
      local-prefixes:
        - go.augendre.info/fatcontext
